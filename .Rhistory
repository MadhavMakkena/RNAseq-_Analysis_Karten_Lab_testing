#saving console output and messages
my_log <- file("my_log.txt")
sink(my_log, append = TRUE, type = "output")
sink(my_log, append = TRUE, type = "message")
cat(readChar(rstudioapi::getSourceEditorContext()$path, file.info(rstudioapi::getSourceEditorContext()$path)$size))
#loading the libraries (only when necessary)
packages <- c("BiocManager", "DESeq2", "ggplot2", "BinfTools", "gpGeneSets", "GSVA", "SAGx", "fgsea", "gage")
invisible(lapply(packages, library, character.only = TRUE))
invisible(lapply(packages, library, character.only = TRUE))
if (any(installed_packages == FALSE)) {
install.packages(packages[!installed_packages])
}
invisible(lapply(packages, library, character.only = TRUE))
packageVersion('BiocManager')
#loading the count data (already in a single file)
count_data <- read.csv("combined_single.csv", header = TRUE, sep = ",", row.names = "ENSGene")
head(count_data, 10)
#setting the directory
setwd("/Users/madhavmakkena/Downloads/RNAseq")
#setting the directory
setwd("/Users/madhavmakkena/Downloads/RNAseq")
#saving console output and messages
my_log <- file("my_log.txt")
sink(my_log, append = TRUE, type = "output")
sink(my_log, append = TRUE, type = "message")
cat(readChar(rstudioapi::getSourceEditorContext()$path, file.info(rstudioapi::getSourceEditorContext()$path)$size))
#loading the count data (already in a single file)
count_data <- read.csv("combined_single.csv", header = TRUE, sep = ",", row.names = "ENSGene")
head(count_data, 10)
